spring:
  # profiles:
  #  active: dev

  application:
    name: logistica-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://192.168.100.124:8081/realms/tpi-backend
          # issuer-uri: http://keycloak:8080/realms/tpi-backend
          # issuer-uri: http://localhost:8081/realms/tpi-backend
          # issuer-uri: http://host.docker.internal:8081/realms/tpi-backend
  datasource:
    url: jdbc:h2:mem:logisticadb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

# 8080: Gateway    -  8081:Keycloack    -  8082:  ms-clientes    -  8083:  ms-solicitudes    -  8084: ms-logistica
server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health

h2:
  console:
    enabled: true


# CONFIGURACIÓN SWAGGER PARA DESARROLLO
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    display-request-duration: true
    operations-sorter: method    # ✅ VALORES CORRECTOS
    tags-sorter: alpha           # ✅ "alpha" SÍ es válido para tags-sorter


    # Configuración para cuando se accede a través del Gateway
    url: /logistica-service/v3/api-docs
    config-url: /logistica-service/v3/api-docs/swagger-config